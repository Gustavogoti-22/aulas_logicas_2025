function converterMetrosParaCentimetros(metros) {
    let centimetros = metros * 100;
    return centimetros;
}
let resultado = converterMetrosParaCentimetros(2);
console.log(`2 metros são ${resultado} centímetros.`);
